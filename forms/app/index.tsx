import { View } from "react-native";

const HomePage = () => {
  return (
    <View
      style={{ display: "flex", height: "100%" }}
      className="justify-center bg-fill"
    ></View>
  );
};

export default HomePage;
